public class Rectangle {
    int width, length;

    void getdata(int x, int y) {
        width = x;
        length = y;
    }

    int area() {
        int a = width * length;
        return (a);
    }
}
